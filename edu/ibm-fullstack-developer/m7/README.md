### Description
Back-End Apps with Node.js and Express

### m1 demos
- `server.js`: minimal Node HTTP server that responds with "Paradise Nursery says hello!" on port 3000.
- `promise-server.js`: promise resolution/rejection demo using axios (or a built-in mock) at `/demo?mode=resolve|reject`.
